var name = document.getElementById("name_length").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length").innerText = name;
}


var name = document.getElementById("name_length2").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length2").innerText = name;
}


var name = document.getElementById("name_length3").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length3").innerText = name;
}


var name = document.getElementById("name_length4").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length4").innerText = name;
}


var name = document.getElementById("name_length5").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length5").innerText = name;
}


var name = document.getElementById("name_length6").innerText;

if (name.length > 9) {
    name = name.substring(0, 9) + "..";
    document.getElementById("name_length6").innerText = name;
}

